---
applyTo: "ui.apps.structure/**/*.xml"
---

# Instructions for ui.apps.structure

- This module defines the repository structure required before `ui.apps` installs — it must remain embedded first in `all`.
- Do not modify structure definitions without understanding the install path and package dependency impact.
